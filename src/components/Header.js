import React from 'react'
import logo from '../img/logo.svg'

const Header = () => {
    return (
        <header>
            <img src={logo} alt="logo" />
            <h1>notedly</h1>
        </header>
    )
}

export default Header
